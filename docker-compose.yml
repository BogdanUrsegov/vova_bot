version: '3.8'

services:
  redis:
    image: redis:7-alpine
    restart: always
    volumes:
      - redis_data:/data

  bot:
    build: .
    restart: always
    env_file:
      - .env
    depends_on:
      - redis
    environment:
      - REDIS_HOST=redis

volumes:
  redis_data: